export const initFederation = (_manifest?: unknown): Promise<void> =>
  Promise.resolve();

export const loadRemoteModule = (_remoteName: string, _exposedModule: string): Promise<unknown> =>
  Promise.resolve({});
